<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand">StreetArt Bdx</a>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="#">Home</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">Artists</a>
    </ul>
  </div>
</nav>


<div class="p-3" style="background-color: #800020; color: white;">

<p style="color: green"><%= notice %></p>

<h1><%= @artist.name %></h1>

<img src="<%= @artist.photo %>" class="img-fluid" alt="<%= @artist.name %>">
<p><strong>Biography</strong> <%= @artist.description %></p>

  <div>
  <%= link_to "Edit this artist", edit_artist_path(@artist) %> |
  <%= link_to "Back to artists", artists_path %>

  <%= button_to "Delete this artist entry", @artist, method: :delete %>
  </div>
</div>
